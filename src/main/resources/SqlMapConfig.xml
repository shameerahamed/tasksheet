<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMapConfig
        PUBLIC "-//iBATIS.com//DTD SQL Map Config 2.0//EN"
        "http://www.ibatis.com/dtd/sql-map-config-2.dtd">

<sqlMapConfig>
    <properties resource="ibatis.properties"/>

    <!-- there are plenty of other optional settings, see the ibatis-sql-maps doc -->
    <settings
            enhancementEnabled="true"
            useStatementNamespaces="true"
            />

    <transactionManager type="JDBC">
        <dataSource type="SIMPLE">
            <property name="JDBC.Driver" value="com.mysql.jdbc.Driver"/>
            <property name="JDBC.ConnectionURL" value="${JAWSDB_URL}"/>
            <property name="JDBC.Username" value="${JAWS_user}"/>
            <property name="JDBC.Password" value="${JAWS_password}"/>
        </dataSource>
    </transactionManager>

    <sqlMap resource="TaskSheet.xml"/>
    <sqlMap resource="User.xml" />
</sqlMapConfig>

